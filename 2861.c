#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    char s[100];
    while (n--)
    {
        fflush(stdin);
        fgets(s, sizeof(s), stdin);
        printf("gzuz\n");
    }
    return 0;
}